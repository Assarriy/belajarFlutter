import 'package:flutter/material.dart';

class Bab6Page extends StatelessWidget {
  const Bab6Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Bab 6")),
      body: const Center(
        child: Text("Ini adalah halaman Bab 6 (masih kosong)"),
      ),
    );
  }
}
